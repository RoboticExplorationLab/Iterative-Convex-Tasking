function traj_cost(X,Y,f,g,n,m)
	# given an input trajectory and a set of measurements
	# determine the cost that you want to minimize

	# INPUTS:
	# X: toal vector of trajectory (n states)
	# Y: total vector of measurements 
	# f: dynamics function
	# g: cost function 
	# n: number of states per input
	# m: number of outputs per measurement

	# OUTPUTS:

	T = 0
	for k = 1:length(x)



end
